import { Dispatch } from 'react';
import { ModelProps } from '../types/AEMModel';
type Props = {
    path: string;
    forceReload?: boolean;
    isInEditor?: boolean;
    pagePath?: string;
    setModel: Dispatch<ModelProps>;
};
export declare const useEditor: () => {
    updateModel: ({ path, forceReload, pagePath, setModel, isInEditor }: Props) => Promise<void>;
};
export {};
